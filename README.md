Simple development environment for node

# contents

* based on node image
* simple dev utilities: less, vim, jq (json processor), httpie
* AWS cli utilities

# build

```sh
$ docker build -t [name] .
```

# publish

```sh
$ docker push [name]
```
